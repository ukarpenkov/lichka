# Отображаемое имя приложения — Lichka

**Дата:** 2026-05-22
**Промпт/задача:** Сделать так, чтобы приложение называлось с большой буквы — Lichka

## Что сделано
- Обновлено отображаемое имя приложения в Android-ресурсах с `lichka` на `Lichka`
- Проверено, что в `app.json` уже указано `"displayName": "Lichka"` для React Native

## Изменённые файлы
- `android/app/src/main/res/values/strings.xml` — `app_name` → `Lichka`

## Принятые решения
- Менялось только пользовательское имя (`app_name`), а не технические идентификаторы (`applicationId`, `package`, `rootProject.name`, имя JS-компонента в `MainActivity`)
- iOS-проекта в репозитории пока нет — при добавлении нужно будет задать `CFBundleDisplayName` = `Lichka`

## Известные ограничения
- Для применения на устройстве/эмуляторе нужна пересборка Android-приложения

## Тестирование
- Не запускалось (изменение строкового ресурса)
