# Изменение порядка иконок в MessageComposer

**Дата:** 2026-07-03
**Промпт/задача:** Поменять порядок иконок в панели действий: микрофон, периодические напоминания, будильник, напоминание-колокольчик, отправка (самолётик)

## Что сделано
- Изменён порядок иконок в `styles.actions` внутри `MessageComposer.tsx`
- Новый порядок (слева направо): Mic → Repeat → AlarmClock → Bell → Send

## Изменённые файлы
- `src/widgets/message-composer/MessageComposer.tsx` — перестановка JSX-элементов в `<View style={styles.actions}>`

## Принятые решения
- Сохранены все существующие свойства каждой кнопки (disabled, color, onPress, onPressIn)
- AnimatedPressable для микрофона остался с теми же настройками longPress

## Известные ограничения
- Нет

## Тестирование
- Визуально проверить порядок иконок в эмуляторе/на устройстве
