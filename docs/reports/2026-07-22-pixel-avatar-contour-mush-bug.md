# Баг: пиксельный контур → «каша»

**Дата:** 2026-07-22  
**Промпт/задача:** Описать проблему (после преобразований в `feat/pixel-avatar` получается каша, не пиксельный контур) и завести баг в `docs/bugs`.

## Что сделано

- Создан баг-документ `docs/bugs/pixel-avatar-contour-mush.md`: симптомы, repro, ожидаемое vs факт, код, история попыток в ветке, гипотезы, направление фикса.
- Статус бага: **open**.

## Изменённые файлы

- `docs/bugs/pixel-avatar-contour-mush.md` — новый баг
- `docs/reports/2026-07-22-pixel-avatar-contour-mush-bug.md` — этот отчёт

## Принятые решения

- Баг заведён как quality/product defect пайплайна, не как runtime crash (decode/pako уже чинились отдельно).
- В тексте зафиксированы неудачи итераций в ветке, чтобы не повторять те же эвристики без ROI.

## Известные ограничения

- Без прогона на устройстве в этой сессии статус опирается на предыдущие скрины пользователя и offline-рендеры алгоритма.

## Тестирование

- Документ-only; код не менялся.
