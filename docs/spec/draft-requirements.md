# 📔 Journal — оффлайн-дневник в формате «Telegram с самим собой»

## 🧠 Общая идея проекта

Разработать **Android-приложение** для оффлайн-ведения заметок, журналов и личных логов.

### Главная концепция

- приложение ощущается как **«Telegram с самим собой**»;
- пользователь ведёт **тематические журналы**;
- записи создаются в **формате сообщений**;
- **время сообщений нельзя изменять**;
- всё работает полностью **локально без сервера**;
- максимальный акцент на **UX, анимациях, tactile feedback, звуках и эмоциональном опыте** использования.

### Эмоциональный тон приложения

Приложение должно создавать ощущение:

- ✨ **премиальности**;
- 🧠 **продуманности**;
- 🧬 **«живого» интерфейса**;
- 🤝 **глубокого взаимодействия** с пользователем.

---

## 🎯 Цели приложения

### Основные цели

- Быстро фиксировать мысли, действия, тренировки, обучение.
- Вести долгосрочные журналы.
- Создавать привычку ежедневного логирования.
- Работать полностью оффлайн.
- Давать ощущение приватности и контроля над данными.

### Эмоциональные цели

Пользователь должен:

- получать удовольствие от интерфейса;
- ощущать **«физичность»** приложения;
- **кайфовать от анимаций**;
- чувствовать **внимание к мелочам**.

---

## 📱 Платформа

### Первая версия

- Android
- Google Play

### Технологии


| Категория         | Технологии                           |
| ----------------- | ------------------------------------ |
| Framework         | React Native                         |
| Язык              | TypeScript                           |
| Навигация         | React Navigation                     |
| Анимации          | React Native Reanimated              |
| Жесты             | React Native Gesture Handler         |
| Хранение          | SQLite + MMKV / AsyncStorage         |
| Нативные модули   | Kotlin (при необходимости)           |
| Аудио (воспроизведение) | react-native-track-player      |
| Аудио (запись)    | react-native-audio-api              |
| Изображения       | react-native-image-picker            |
| Тактильный отклик | react-native-haptic-feedback         |


---

## 🏗 Архитектура приложения

### Рекомендуемый подход

- **Feature-Sliced Design (FSD)**  
или  
- **модульная feature-based архитектура**

### Основные слои

app
├── screens
├── widgets
├── features
├── entities
└── shared

---

## 📝 Типы заметок

### 5.1 Журналы (главная сущность)

**Формат** — как чат Telegram:

- сообщения идут по времени;
- **нельзя менять timestamp**;
- можно редактировать текст, но **дата создания остаётся оригинальной**.

#### Примеры журналов

- Обучение
- Тренировки
- Работа
- Дневник
- Привычки
- Изучение языков

---

### 5.2 Общие заметки

Более сложный формат заметок с поддержкой:

- обычного текста;
- ✅ чекбоксов;
- 📋 списков;
- 📊 таблиц;
- **markdown-lite**;
- ⏰ напоминаний;
- 📦 вложенных блоков.

---

## ⚙️ Основной функционал

### 6.1 Журналы

#### Возможности

- создание журнала;
- выбор названия;
- выбор иконки;
- выбор цвета;
- создание сообщений;
- удаление сообщений;
- поиск по сообщениям;
- фильтрация;
- статистика активности.

#### Ограничения

- timestamp **нельзя редактировать**;
- нельзя подделывать дату записи через UI;
- записи сортируются строго по времени создания.

#### Формат отображения

Как чат:

- сообщения;
- дата;
- время;
- анимация появления;
- плавный скролл;
- sticky dates.

---

### 6.2 Сообщения

#### Типы сообщений

- текст;
- ссылки;
- изображения;
- голосовые заметки;
- код *(future)*.

#### Поведение

- быстрое создание;
- instant save;
- autosave;
- offline-first.

---

### 6.2.1 Голосовые заметки

#### Описание

Запись голоса — обязательная функция MVP. Пользователь может создать голосовое сообщение прямо из чата журнала, аналогично голосовым в Telegram.

#### Возможности

- запись аудио с микрофона;
- визуализация уровня звука в реальном времени (waveform);
- отправка / отмена записи;
- воспроизведение внутри чата;
- отображение длительности сообщения.

#### Требования

- запись хранится локально в формате AAC / MP3;
- файл привязывается к сообщению в SQLite;
- максимальная длительность записи — без ограничений (или до 30 минут);
- мгновенный старт записи (без задержек);
- haptic feedback при начале и завершении записи.

#### Технологии

| Задача              | Решение                          |
| ------------------- | -------------------------------- |
| Запись аудио        | react-native-audio-api          |
| Воспроизведение     | react-native-track-player        |
| Waveform            | react-native-audio-api / Skia    |
| Хранение файлов     | локальная файловая система       |

---

### 6.2.2 Изображения

#### Описание

Поддержка вставки изображений в сообщения журнала. Поведение — как в Telegram: изображение прикрепляется к сообщению, отображается inline в ленте чата.

#### Возможности

- выбор изображения из галереи;
- съёмка через камеру;
- превью в ленте чата;
- полноэкранный просмотр по тапу;
- pinch-to-zoom.

#### Требования

- изображения хранятся локально;
- в базе данных сохраняется путь к файлу и метаданные (размер, дата);
- сжатие при сохранении (качество по умолчанию — 80%);
- поддержка форматов: JPEG, PNG, WebP.

#### Технологии

| Задача              | Решение                          |
| ------------------- | -------------------------------- |
| Выбор изображения   | react-native-image-picker        |
| Камера              | react-native-camera / expo-camera |
| Сжатие              | react-native-image-resizer       |
| Просмотр            | react-native-image-viewing       |

---

### 6.3 Общие заметки

#### Возможности

- rich text;
- чекбоксы;
- таблицы;
- markdown;
- блоки;
- drag-and-drop секций;
- закрепление заметок.

---

### 6.4 Напоминания

#### Тип 1: Будильник

**Концепция:** уведомление работает как будильник — не исчезает, пока пользователь не примет действие. Подходит для важных напоминаний, которые нельзя пропустить.

**Особенности:**

- полноэкранный экран уведомления;
- звук играет непрерывно до реакции пользователя;
- вибрация с нарастающей интенсивностью;
- блокировка интерфейса до взаимодействия;
- работает даже при заблокированном экране (full-screen intent).

**Действия:**

- отложить (snooze) — с настраиваемым интервалом;
- выключить (dismiss);
- открыть связанную заметку / журнал.

**Настройки:**

- выбор мелодии;
- настройка громкости;
- snooze-интервал (5 / 10 / 15 / 30 минут);
- максимальная длительность звука (по умолчанию — до реакции, максимум 5 минут непрерывного звука).

#### Примеры использования

- напоминание о тренировке;
- дедлайн по задаче;
- важное событие в журнале.

#### Тип 2: Push-уведомление

- локальное уведомление;
- тихий режим;
- вибрация;
- deep-link в заметку.

---

### 6.5 Циклические напоминания

**Поддержка:**

- каждые X минут;
- каждый день;
- каждую неделю;
- кастомные интервалы.

**Примеры:**

- каждые 15 минут;
- каждый день в 9:00;
- каждые 2 часа.

---

### 6.6 Темы

**Поддержка:**

- светлая тема;
- тёмная тема;
- auto system theme.

**Особенности:**

- отдельные анимации под тему;
- отдельные звуки;
- разные вибрационные паттерны.

---

### 6.7 Локальное хранение

#### База данных

**SQLite**

#### Что хранится

- журналы;
- сообщения;
- настройки;
- напоминания;
- темы;
- статистика.

#### Требования

- всё локально;
- без сервера;
- приложение работает **без интернета**.

---

### 6.8 Импорт / Экспорт

#### Экспорт

- JSON файл;
- ZIP архив;
- ручной перенос.

#### Импорт

- выбор JSON;
- восстановление данных;
- merge / import options.

---

### 6.9 Шифрование

**Идея**

Пользователь вводит:

- ключ;
- фразу;
- пароль.

**Пример:** `яблоко1344`

#### Поведение

- JSON шифруется;
- файл невозможно прочитать напрямую;
- при импорте нужен тот же ключ.

#### Рекомендуемая реализация

- AES-256;
- PBKDF2;
- crypto-js или native encryption.

---

### 6.10 Google Drive Sync *(optional)*

**Назначение** — резервное копирование и перенос данных между устройствами.

**Концепция** — как резервное копирование WhatsApp: пользователь вручную включает синхронизацию, данные загружаются в Google Drive и восстанавливаются на другом устройстве.

**Особенности:**

- опциональная функция — включается в настройках вручную;
- не постоянная синхронизация — только по запросу пользователя;
- резервные копии в зашифрованном виде;
- выбор конкретных журналов для бэкапа;
- восстановление из Google Drive на новом устройстве;
- отображение даты последнего бэкапа;
- push-уведомление при успешном завершении.

**Настройки:**

- привязка Google-аккаунта;
- автобэкап (опционально): ежедневно / еженедельно / вручную;
- шифрование бэкапа (пароль);
- учёт трафика — только по Wi-Fi (опционально).

**Ограничения:**

- не поддерживает real-time sync;
- не работает как облачное хранилище;
- конфликты решаются в пользу последнего бэкапа.

---

### 6.11 Dynamic Island *(Android)*

**Концепция** — приложение поддерживает современные Android-компоненты для отображения активных процессов поверх интерфейса, аналогично Dynamic Island на iOS.

**Реализация:**

- **Live Notifications** (Android 13+): отображение текущих напоминаний и таймеров в статус-баре;
- **Heads-up notifications**: всплывающие уведомления для будильников;
- **Always-on Display** *(опционально)*: отображение ближайшего напоминания на экране блокировки.

**Примеры:**

- активный будильник — индикатор в статус-баре;
- идёт запись голоса — индикатор записи;
- циклическое напоминание — pulsing notification dot.

**Технологии:**

| Задача                    | Решение                              |
| ------------------------- | ------------------------------------ |
| Live Notifications        | Android NotificationCompat + Foreground Service |
| Heads-up                  | Android NotificationChannel (high priority) |
| Always-on Display         | Android Widget / Complications API   |

---

### 6.12 Google Calendar — сообщение себе *(optional)*

**Назначение** — дать пользователю возможность **отправить себе заметку в Google Calendar**: зафиксировать мысль, напоминание или сообщение из журнала как событие в личном календаре, чтобы увидеть его в привычном календарном контексте (телефон, веб, виджеты Google).

**Концепция** — расширение идеи «Telegram с самим собой»: пользователь не только пишет себе в приложении, но при необходимости **дублирует запись в Google Calendar** как личное событие «себе».

#### Возможности

- создать **сообщение себе** в Google Calendar из:
  - нового текста (быстрый ввод);
  - существующего сообщения журнала;
  - напоминания / общей заметки;
- указать **дату и время** события (или «весь день»);
- задать **заголовок и описание** (тело заметки);
- опционально — **напоминание** календаря (за 5 / 10 / 30 минут и т. п.);
- открыть созданное событие в приложении Google Calendar (deep link / intent);
- отображать статус: «добавлено в календарь» / «ожидает синхронизации» / «ошибка».

#### Поведение

- функция **опциональна** — включается в настройках после привязки Google-аккаунта;
- локальная запись в журнале **сохраняется первичной**; экспорт в Calendar — дополнительное действие, не замена локальных данных;
- при отсутствии сети — **очередь отложенных операций** с отправкой после подключения;
- повторная отправка одной и той же записи не создаёт дубликат без явного подтверждения пользователя (или помечается связь `calendar_event_id` в локальной БД).

#### Ограничения

- не real-time двусторонняя синхронизация с Calendar (только **создание / обновление по запросу** пользователя);
- не импорт всех событий Google Calendar в приложение на первом этапе;
- события создаются в **основном календаре пользователя** или в выбранном календаре из списка доступных (настройка).

#### Связь с напоминаниями

- из напоминания приложения можно **«положить в Google Calendar»** — событие с тем же временем и текстом;
- локальный будильник / push в приложении и событие в Calendar **независимы**: пользователь выбирает, нужны оба канала или один.

#### Технологии

| Задача              | Решение                          |
| ------------------- | -------------------------------- |
| OAuth / аккаунт     | Google Sign-In (общий с Drive Backup) |
| Calendar API        | Google Calendar API v3             |
| Создание события    | `events.insert`                    |
| Deep link           | `content://` / Intent в Google Calendar |

#### Permissions (Scopes)

Дополнительно к Drive Backup (если включён):

- `https://www.googleapis.com/auth/calendar.events` — создание и изменение событий в календарях пользователя.

Scope запрашивается **только при включении** интеграции с Calendar, с понятным объяснением в UI.

---

## 🎨 UX/UI концепция

### 7.1 Главный принцип

Приложение должно ощущаться:

- дорогим;
- живым;
- tactile;
- fluid.

При этом **визуальный слой намеренно сдержанный** (см. §7.1.2): ощущение **премиальности** и глубины достигается **анимацией, физикой взаимодействия, тактильным и звуковым откликом**, а не перегрузкой цветом и декоративными контролами.

### 7.1.1 Иконка приложения

**Требование:** иконка приложения — монохромная.

**Концепция:**

- один цвет (или оттенки одного цвета);
- минималистичный дизайн;
- узнаваемость на любом фоне;
- соответствие стилю Material You (Android 12+).

**Адаптивность:**

- поддержка adaptive icon (Android 8+);
- автоматическая подстройка под системную палитру (Material You dynamic color);
- отдельные версии для тёмной и светлой темы.

**Форматы:**

| Тип          | Размер        |
| ------------ | ------------- |
| Adaptive     | 108×108 dp    |
| Legacy       | 48×48 dp      |
| Round        | 108×108 dp    |
| Foreground   | 66×66 dp (внутри adaptive) |

### 7.1.2 Визуальный язык, палитра и скорость задачи

**Роль дизайна в продукте:** интерфейс трактуется как **инструмент быстрой фиксации** мыслей и напоминаний. Визуальная система должна снижать когнитивную нагрузку и время принятия решения, а не конкурировать с контентом за внимание.

**Палитра и иерархия**

- опираемся на **узкую палитру — по сути 2–3 семантических цвета** в типичном состоянии экрана (фон поверхности, основной текст и действия, при необходимости один акцент или системный цвет состояния — успех, ошибка, отключено);
- **базовый светлый режим по умолчанию:** светлый/белый фон, **основные действия и ключевой текст — тёмные (в т.ч. чёрные)** для максимального контраста и предсказуемости без «украшательств»;
- тёмная тема и адаптация под Material You **не отменяют** принципа малого числа семантических цветов: системные динамические оттенки используются дисциплинированно, без размывания ролей «фон / текст / действие».

**Контролы и «хром» интерфейса**

- **первичные действия** по возможности оформляются как **текстовые кнопки (text / label buttons)** без декоративной обводки: пользователь считывает намерение по подписи и контексту, а не по «рамке»;
- отсутствие border у типовых кнопок — осознанный выбор в пользу **скорости сканирования** и визуальной тишины; при необходимости границы и заливки используются для **вторичных** действий или редких состояний (disabled, outline-secondary), чтобы не дублировать визуальный вес всего экрана;
- **иконки** допускаются **выборочно**: там, где они заменяют длинную подпись, дают мгновенное узнавание (навигация, системные действия) или поддерживают международную аудиторию; массовое заполнение экрана иконками без текстовой опоры не является целевым паттерном.

**Дата, время и напоминания**

- сценарий **«записать напоминание с датой/временем»** — **критический путь UX**: требуется **оригинальный, но предельно быстрый** способ ввода даты и времени (комбинация умных пресетов, компактного picker, подсказок по контексту, опционально естественного ввода — конкретная механика задаётся отдельной проработкой);
- целевой ориентир — **минимум касаний и экранов** от намерения до подтверждённого напоминания без потери точности и без forcing-потоков, которые заставляют пользователя «воевать» с календарём.

**Согласование с остальной концепцией**

- «Живость» и fluid из §7.1 **не противоречат** минимализму хрома: микровзаимодействия, переходы и пружины усиливают ощущение качества **без** усложнения статической картинки;
- при конфликте приоритетов между **украшением статического UI** и **сокращением пути пользователя** выбирается второе.

### 7.2 Анимации

**Требование** — максимально использовать:

- Reanimated;
- Gesture Handler;
- shared transitions;
- physics animations.

**Примеры:**

- живой скролл;
- elastic interactions;
- инерция;
- spring-анимации;
- depth;
- liquid transitions;
- parallax;
- animated blur;
- animated gradients.

### 7.3 Нативные анимации

Разрешается:

- писать Kotlin modules;
- использовать native rendering;
- использовать Android shaders;
- использовать Skia.

### 7.4 Haptic feedback

Каждое действие должно:

- ощущаться;
- подтверждаться;
- иметь физический отклик.

**Примеры:**

- мягкий tap;
- success vibration;
- long press impact;
- error vibration.

### 7.5 Sound Design

**Требования** — звуки:

- глубокие;
- минималистичные;
- не раздражающие;
- premium feeling.

**События для звуков:**

- создание записи;
- завершение действия;
- будильник;
- reminder;
- interaction feedback.

---

## 🌐 Многоязычность

### На старте

- русский
- английский

### В будущем

- немецкий
- японский
- турецкий
- индонезийский

### Технологии

- i18next
- react-i18next

---

## ⚡ Производительность

### Требования

- 60 FPS минимум;
- target 120 FPS animations;
- быстрый запуск;
- минимальный memory usage;
- virtualized lists.

### Оптимизации

- FlashList;
- memoization;
- worklets;
- native drivers;
- lazy rendering.

---

## 🔒 Безопасность

**Требования:**

- локальные данные;
- encrypted backups;
- отсутствие обязательной регистрации;
- отсутствие сервера.

---

## 🚀 Будущие функции

Возможные фичи:

- Google Calendar — сообщение себе (см. §6.12);
- widgets;
- AI summaries;
- analytics;
- streak system;
- achievements;
- tags;
- graph visualization;
- mood tracking.

---

## 🧩 Ограничения MVP

### В MVP **НЕ делать**

- аккаунты;
- сервер;
- realtime sync;
- соцсеть;
- cloud backend;
- web version.

---

## ✅ MVP Scope

### MVP должен включать (обязательно)


| Компонент               | Статус |
| ----------------------- | ------ |
| журналы                 | ✅      |
| сообщения               | ✅      |
| голосовые заметки       | ✅      |
| изображения             | ✅      |
| SQLite                  | ✅      |
| локальные уведомления   | ✅      |
| будильник-уведомления   | ✅      |
| тёмная/светлая тема     | ✅      |
| экспорт/import JSON     | ✅      |
| базовое шифрование      | ✅      |
| анимации                | ✅      |
| haptic feedback         | ✅      |
| мультиязычность         | ✅      |
| монохромная иконка      | ✅      |


### Можно позже

- Google Drive Backup / Sync — см. раздел «Авторизация и Google Drive Backup».
- Google Calendar — сообщение себе — см. §6.12.
- таблицы;
- rich editor;
- widgets;
- Dynamic Island (Live Notifications).

---

## 🧭 Навигация приложения

### Основные экраны


| Экран                 | Описание                   |
| --------------------- | -------------------------- |
| 🏠 **Home**           | Список журналов            |
| 📱 **Journal Screen** | Чатоподобный экран журнала |
| 📄 **General Notes**  | Список общих заметок       |
| ⏰ **Reminder Center** | Все напоминания            |
| ⚙️ **Settings**       | Настройки приложения       |


---

## 🔐 Авторизация и Google Drive Backup

*Требования для версии после MVP; детализуют пункт «Google Drive Backup / Sync» в блоке «Можно позже».*

### Авторизация через Google

Приложение должно поддерживать **вход пользователя через Google-аккаунт**.

#### Функционал

- вход через Google;
- выход из аккаунта;
- автоматическое восстановление сессии;
- отображение текущего Google-аккаунта.

#### Технологии

- Google OAuth 2.0;
- Google Sign-In для React Native.

#### Permissions (Scopes)

Использовать **только** scope:

`https://www.googleapis.com/auth/drive.appdata`

Это даёт доступ **только** к скрытой папке приложения в Google Drive (appDataFolder).

---

### Google Drive Backup

#### Назначение

Приложение должно сохранять **backup пользовательских данных** в Google Drive пользователя.

#### Требования

- backup хранится в **appDataFolder**;
- файлы backup **скрыты от пользователя** в интерфейсе Drive;
- приложение **не имеет доступа** к остальным файлам Google Drive пользователя;
- backup **привязан** к Google-аккаунту пользователя.

#### Функции backup

Приложение должно поддерживать:

- создание backup;
- обновление backup;
- удаление старого backup;
- восстановление backup;
- автоматическую синхронизацию.

#### Формат хранения

Backup хранится в формате **JSON**.

Пример:

```json
{
  "version": 1,
  "updatedAt": 1710000000,
  "data": {}
}
```

#### Автоматическая синхронизация

Приложение должно:

- автоматически **загружать** backup после изменений;
- **проверять** наличие backup при запуске;
- **восстанавливать** данные после входа пользователя.

#### Безопасность

**Требования:**

- использовать только **OAuth-авторизацию** Google;
- **не хранить** пароли пользователя;
- хранить **access token** безопасно (защищённое хранилище ОС / best practices платформы);
- использовать **HTTPS** для запросов;
- **не использовать** полный доступ к Google Drive (только `drive.appdata`).

#### Offline-поддержка

Приложение должно:

- работать **без интернета** (локальные операции и очередь изменений);
- выполнять **синхронизацию** после подключения к сети.

#### Обработка ошибок

Необходимо обрабатывать:

- отсутствие интернета;
- ошибки авторизации;
- истёкший access token (обновление токена / повторная авторизация);
- ошибки Google Drive API;
- отсутствие backup-файла;
- повреждённый backup (невалидный JSON, несовместимая `version`, и т. п.).

---

### Google Calendar — сообщение себе

*Требования для версии после MVP; детализирует §6.12.*

#### Назначение

После входа через Google пользователь может **добавлять личные заметки и сообщения в свой Google Calendar** — как события «себе», без выхода из концепции приватного оффлайн-дневника.

#### Функционал

- включение / отключение интеграции в настройках;
- выбор целевого календаря (по умолчанию — primary);
- действие «Отправить в Calendar» из сообщения, напоминания или отдельного экрана быстрого ввода;
- сохранение связи локальной записи с `calendarEventId` для последующего обновления или отмены;
- повторная авторизация при отзыве scope `calendar.events`.

#### Permissions (Scopes)

При включении Calendar-интеграции дополнительно к `drive.appdata` (если используется backup):

`https://www.googleapis.com/auth/calendar.events`

#### Offline-поддержка

- создание и редактирование записи в приложении — **без интернета**;
- отправка в Google Calendar — **при наличии сети** или из очереди после восстановления соединения.

#### Обработка ошибок

- отсутствие интернета (очередь);
- отказ в доступе к календарю;
- удалённый или недоступный календарь;
- конфликт при обновлении уже изменённого в Calendar события (предложить перезаписать или создать новое).

---

## 📦 Структура хранения SQLite (пример)

```sql
-- Журналы
CREATE TABLE journals (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  icon TEXT,
  color TEXT,
  created_at INTEGER NOT NULL
);

-- Сообщения
CREATE TABLE messages (
  id TEXT PRIMARY KEY,
  journal_id TEXT NOT NULL,
  content TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  updated_at INTEGER,
  FOREIGN KEY (journal_id) REFERENCES journals(id) ON DELETE CASCADE
);

-- Общие заметки
CREATE TABLE general_notes (
  id TEXT PRIMARY KEY,
  title TEXT,
  content TEXT,
  is_pinned INTEGER DEFAULT 0,
  created_at INTEGER NOT NULL,
  updated_at INTEGER
);

-- Напоминания
CREATE TABLE reminders (
  id TEXT PRIMARY KEY,
  target_id TEXT, -- journal_id или note_id
  target_type TEXT, -- 'journal' или 'note'
  type TEXT, -- 'alarm' или 'push'
  schedule TEXT, -- cron-like или интервал
  enabled INTEGER DEFAULT 1
);



```

## 🎮 Пример тактильной обратной связи

| Действие | Вибрация |
|----------|----------|
| Tap по кнопке | light (10ms) |
| Создание сообщения | success + soft pop |
| Удаление | warning + long press |
| Ошибка | error (двойная) |
| Будильник | непрерывная до реакции |

---

## 🔊 Пример звуковой схемы

| Событие | Звук |
|---------|------|
| Отправка сообщения | мягкий «пуф» |
| Открытие журнала | глубокий «вдох» |
| Завершение задачи | «динь» с затуханием |
| Будильник | постепенно нарастающий |


